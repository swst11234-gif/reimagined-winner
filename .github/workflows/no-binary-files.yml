name: no-binary-files

on:
  push:
  pull_request:

jobs:
  validate-no-binary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Validate repository has no binary files
        run: |
          python3 scripts/check_no_binary.py
          python3 scripts/check_no_binary.py --history
